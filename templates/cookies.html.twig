{% set rightnow = "now"|date("Y-m-d H:i:s") %}
<p>Time: {{ rightnow|date("Y-m-d H:i:s", "America/Denver") }}</p>
{% set hour = "now"|date("G", "America/Denver") %}
{% if hour >= 9 and hour < 17 %}
    <p>Time for cookies!</p>
{% else %}
    <p>Time to bake more cookies!</p>
{% endif %}